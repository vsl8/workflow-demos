name: w5-failure-function-demo

on: workflow_dispatch

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Fail intentionally
        run: exit 1

      - name: Run only if previous step failed
        if: ${{ failure() }}
        run: echo "A previous step failed"
